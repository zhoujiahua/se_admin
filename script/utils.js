/**
 *  Utility methods
 */
;
(function ($, $win, $doc) {
    // A static method
    $.extend({
        localSet: function () {},
        localGet: function () {},
        localRemove: function () {}
    })

    // Prototype method
    $.fn.extend({
        layout: function (elem, style) {}
    })

}(jQuery, window, document))